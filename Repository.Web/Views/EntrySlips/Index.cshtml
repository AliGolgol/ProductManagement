@model IEnumerable<Repository.DomainModel.Entry.EntrySlip>

@{
    ViewBag.Title = "Index";
}

<<p>
    @Html.ActionLink("ایجاد", "Create", null, new { @class = "btn btn-primary btn-lg btn-block " })
</p>
<div class="panel panel-primary">
    <!-- Default panel contents -->
    <div class="panel-heading">

    </div>
    <div class="panel-body">
        <p>...</p>
        <table class="table table-hover table-bordered table-striped">
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Period.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CreatedDate)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Description)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.DeliveryMan)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UserId)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.EntrySlipType.Name)
                </th>
                <th></th>
            </tr>

            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Period.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CreatedDate)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Description)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DeliveryMan)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UserId)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EntrySlipType.Name)
                    </td>
                    <td>
                        @Html.ActionLink("ویرایش", "Edit", new { id = item.Id }, new { @class = "btn btn-warning  " }) |
                        @Html.ActionLink("جزئیات", "Details", new { id = item.Id }, new { @class = "btn btn-default  " }) |
                        @Html.ActionLink("حذف", "Delete", new { id = item.Id }, new { @class = "btn btn-danger  " })
                    </td>
                </tr>
            }

        </table>
    </div>
    <!-- Table -->
    <div class="panel-footer"></div>
</div>